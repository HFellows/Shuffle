(ns shuffle.core
  (:require [shuffle.random :as rand]
            [shuffle.card :as card]))

(defn foo
  "I don't do a whole lot."
  [x]
  (println x "Hello, World!"))

(defn -main
  "Run"
  [& args])
